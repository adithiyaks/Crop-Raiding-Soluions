// const int buzzerPin = 9;

// void setup() {
//     pinMode(buzzerPin, OUTPUT);
//     Serial.begin(9600);  // Make sure it matches the baud rate in Python script
// }

// void loop() {
//   void Buzzer(){
//       if (Serial.available() > 0) {
//           char receivedChar = Serial.read();
//           if (receivedChar == 'D') {
//               // Code to emit deer sound
//               tone(buzzerPin, 1000);  // Example tone for deer
//               delay(1000);  // Duration
//               noTone(buzzerPin);  // Turn off the buzzer
//           } else if (receivedChar == 'E') {
//               // Code to emit elephant sound
//               tone(buzzerPin, 2000);  // Example tone for elephant
//               delay(1000);  // Duration
//               noTone(buzzerPin);  // Turn off the buzzer
//           }
//       }
//     }// Other code in loop, if any
// }
